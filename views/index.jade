extends layout

block content
  .container-fluid
    if !user
      h1 ALIS Web Portal

      include login-partial.jade
      //-a.btn.btn-primary(href='/auth/twitter') Sign in with Twitter
    else
      h1 Welcome!